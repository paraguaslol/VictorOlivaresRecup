<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.victor.victorolivaresrec">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Usuaris.Activity_Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Usuaris.Activity_Registrar" />
        <activity
            android:name=".App_Main"
            android:label="@string/title_activity_app__main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Adapters.Usuaris_Adapter"></activity>
    </application>

</manifest>